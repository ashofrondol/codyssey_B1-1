# Codyssey B1-1 평가문항 답변 정리

> 작성자: 정세영 · 작성일: 2026-05-13
> 본 문서는 codyssey 평가 시스템의 5개 항목(평가문항)에 대한 답변을 정리한 것이다.
> 근거 파일: [src/monitor.sh](../../src/monitor.sh), [요구사항_수행_내역서.md](요구사항_수행_내역서.md), `bin/agent-app` (운영 측 제공 바이너리, Ubuntu 24.04 전용), [src/report.sh](../../src/report.sh), [src/archive_logs.sh](../../src/archive_logs.sh), `src/01_*.sh ~ 07_*.sh` (setup 7단계)

---

## 항목 1 — 구현 완성 체크리스트

> 요구사항 충족 여부를 항목별로 정리한다. 모두 **"예"** 이며, 근거를 함께 첨부한다.

### 1-1. SSH 포트가 20022로 변경되었고, Root 원격 접속이 차단되었는가? — ✅ 예

`/etc/ssh/sshd_config`에서 다음 두 줄을 변경:

```
Port 20022
PermitRootLogin no
```

검증:

```bash
$ sudo grep -E '^(Port|PermitRootLogin)\b' /etc/ssh/sshd_config
Port 20022
PermitRootLogin no

$ sudo ss -tulnp | grep ':20022'
tcp   LISTEN 0      128                0.0.0.0:20022      0.0.0.0:*    users:(("sshd",pid=3951,fd=3))           
tcp   LISTEN 0      128                   [::]:20022         [::]:*    users:(("sshd",pid=3951,fd=4))
```

### 1-2. 방화벽이 활성화되어 있고, 20022/tcp · 15034/tcp만 허용되는가? — ✅ 예 (UFW 선택)

```bash
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 20022/tcp comment 'SSH'
sudo ufw allow 15034/tcp comment 'AGENT APP'
sudo ufw --force enable
```

`ufw status verbose` 결과에 `Status: active` + 위 두 규칙만 노출됨.

### 1-3. agent-admin/dev/test 계정과 agent-common/core 그룹이 요구사항대로 구성되어 있는가? — ✅ 예

| 그룹 | 멤버 | 의미 |
|---|---|---|
| `agent-common` | admin, dev, test | 공용 작업 공간 공유 |
| `agent-core`   | admin, dev | 비밀(키)/로그 접근 가능 |

검증: `id agent-admin / agent-dev / agent-test`로 각 계정의 소속 그룹 확인 완료.

### 1-4. Boot Sequence 5단계 [OK] 통과 + "Agent READY" 출력 — ✅ 예

```
[1/5] Checking User Account             [OK]
[2/5] Verifying Environment Variables   [OK]
[3/5] Checking Required Files           [OK]
[4/5] Checking Port Availability        [OK]
[5/5] Verifying Log Permission          [OK]
------------------------------------------------------------
Agent READY
```

각 단계의 검사 항목: ① 비-root UID, ② 환경변수 완전성, ③ 키 파일 존재·내용, ④ 15034 포트 가용성, ⑤ 로그 폴더 쓰기 권한.

### 1-5. monitor.sh가 프로세스/포트 점검 후 비정상 시 `exit 1`로 종료되는가? — ✅ 예

[monitor.sh](../../src/monitor.sh) §1 Health Check 블록에서:

- 프로세스(`pgrep -x`) 미존재 → `[FAIL]` 출력 후 `exit 1`
- 포트(`ss -tlnH`) LISTEN 아님 → `[FAIL]` 출력 후 `exit 1`

### 1-6. `/var/log/agent-app/monitor.log`가 지정 포맷으로 누적 기록되는가? — ✅ 예

monitor.sh §5에서 한 줄씩 append:

```
[2026-05-13 21:35:01] PID:4445 CPU:0.0% MEM:3.9% DISK_USED:1%
```

포맷: `[YYYY-MM-DD HH:MM:SS] PID:<n> CPU:<x>% MEM:<x>% DISK_USED:<x>%`

### 1-7. cron 매분 실행으로 monitor.log가 자동 증가하는가[^1]? — ✅ 예

agent-admin의 crontab에 다음 등록:

```
* * * * * AGENT_HOME=... AGENT_PORT=15034 AGENT_LOG_DIR=/var/log/agent-app /home/agent-admin/agent-app/bin/monitor.sh >> /home/agent-admin/monitor.cron.log 2>&1
```

검증: 70초 간격으로 `wc -l`을 두 번 실행 시 라인 수가 1~2 증가함.

### 1-8. monitor.log 용량 관리(10MB/10개)가 설정·동작하는가? — ✅ 예

monitor.sh §6에서 자체 구현된 size-based rotation:

- `MAX_LOG_SIZE=$((10 * 1024 * 1024))`, `MAX_LOG_FILES=10`
- 매 실행 시 `stat -c %s`로 현재 크기 확인
- 10MB 이상이면 `.10`을 삭제 → `.9 → .10`, `.8 → .9`, …, `.1 → .2`로 한 칸씩 밀고
- 현재 파일을 `.1`로 이동 후 새 빈 파일 생성

---

## 항목 2 — 구현 방식 설명

### 2-1. 프로세스 식별(`pgrep`) · 포트 확인(`ss`) 명령과 선택 이유

**프로세스 식별 — `pgrep -x "agent-app" | head -n1`**

- `pgrep`은 이름으로 PID를 바로 출력해 `ps aux | grep | grep -v grep | awk` 같은 파이프 체인을 없애 준다.
- `-x`는 **프로세스 이름(comm)과 정확히 일치[^2]**하는 것만 잡는다 (`-f` 처럼 cmdline 전체를 보지 않음). 우리 케이스에서는 `agent-app` 이라는 문자열이 디렉토리 경로(`/home/agent-admin/agent-app/...`)에도 등장하므로 `-f` 를 쓰면 `monitor.sh` 자신을 매칭하는 자기참조 문제가 생긴다 → `-x` 가 안전.
- `head -n1`은 다중 매치 시 첫 PID만 사용해 출력 일관성 확보.

**포트 확인 — `ss -tlnH` (없으면 `netstat -tln`로 폴백)**

- `ss`는 커널의 socket diagnostic netlink를 직접 읽어[^3] **`netstat`보다 빠르고 정확**하다 (Ubuntu 18+ 기본 도구).
- 옵션 의미:
  - `-t` TCP만, `-l` LISTEN 상태만, `-n` 이름 해석 없이 숫자(빠름), `-H` 헤더 생략[^4](awk가 편해짐)
- `command -v ss`로 존재 여부를 체크해 미설치 환경에선 netstat로 fallback — **이식성** 확보.
- `awk -v p=":15034" '$4 ~ p {print "Y"; exit}'`로 `0.0.0.0:15034`/`[::]:15034`를 모두 매치.

### 2-2. CPU/MEM/DISK 추출·파싱 방식과 로그 포맷 고정 이유

**추출 방식**

| 자원 | 명령 | 파싱 방식 |
|---|---|---|
| CPU | `top -bn2 -d 1` 2회 샘플 중 **두 번째** | `Cpu(s)` 줄에서 `id`(idle) 직전 값 추출, `100 - idle`로 사용률 산출[^5]. 1회 샘플(`-bn1`)은 부팅 이후 누적 평균에 가까워 "지금"의 사용률이 아니므로 두 번째 샘플만 `END {print v}` 로 채택한다 |
| MEM | `free` | `Mem:` 줄에서 `$3/$2*100` (used/total) |
| DISK | `df -P /` | root 파티션의 5번째 컬럼(%)에서 `%` 제거 |

**소수점·기본값 처리**

- `printf "%.1f"`로 소수 첫째 자리에 고정.
- 파싱 실패 시 `[[ -z ... ]]`로 `"0.0"`/`"0"` 기본값 — 로그가 비지 않도록 방어.

**임계값 비교는 `awk -v ... 'BEGIN {exit !(v+0 > t+0)}'`**

- bash의 `[[ ]]`는 정수만 비교 가능하므로 소수 비교에 `awk`를 사용[^6].

**로그 포맷을 `[ts] PID:n CPU:x% MEM:x% DISK_USED:x%`로 고정한 이유**

- **고정 토큰 + 사람 가독성 동시 충족**: `[2026-05-13 21:35:01]` 부분은 정렬·정렬 가능, `KEY:VALUE` 구조는 `awk -F'[: ]'`로 후처리 쉬움.
- 한 줄 한 레코드 (newline-delimited) → `tail`, `grep`, `wc -l` 같은 표준 도구로 즉시 분석 가능.
- `report.sh`가 이 포맷을 가정하고 `awk` 한 번으로 평균/최대/최소를 산출.

### 2-3. 소유자 vs 실행자 권한 정책 — 어떻게 만족시켰는가

| 대상 | 소유자 | 그룹 | 권한 | 실행자가 가능한 이유 |
|---|---|---|---|---|
| `$AGENT_HOME/bin/monitor.sh` | agent-dev | agent-core | `-rwxr-x---` (750) | agent-admin이 agent-core 그룹 멤버 → 그룹 read+execute 적용 |
| `/var/log/agent-app/` | root | agent-core | `drwxrwx---+` (770) + ACL | agent-core 멤버는 write 가능, ACL `default`로 신규 파일도 자동 상속[^7] |

**한 줄 요약**: 스크립트 소유는 개발(agent-dev), 실행·로그 기록은 운영(agent-admin)이 하되, 둘 다 **agent-core** 그룹에 묶어 그룹 권한으로 read/write를 허용. 신규 파일도 권한이 풀리지 않도록 ACL `default:group:agent-core:rwx`을 부여.

### 2-4. 로그 용량 관리 — logrotate가 아니라 스크립트로 구현한 이유

**구현 방식**: monitor.sh §6에서 자체 size-based rotation (10MB · 10개 보관).

**왜 logrotate가 아니라 스크립트 자체에서 했나**

| 항목 | logrotate | 스크립트 내장 |
|---|---|---|
| 트리거 시점 | cron.daily/hourly 가 깨워야 함 | **매분 monitor.sh 실행마다 검사** — 폭주 시 더 신속히 차단 |
| 권한 | logrotate 데몬은 root 권한으로 동작, 별도 설정 필요 | monitor.sh 실행 컨텍스트(agent-admin + agent-core) 그대로 사용 |
| 의존성 | `/etc/logrotate.d/agent-app` 추가 설정 + logrotate 패키지 | **추가 시스템 의존성 0** |
| 이식성 | 배포판마다 logrotate 위치·옵션 차이 | bash + coreutils만으로 동일 동작 |

미션 요구사항이 단순(10MB × 10개)하고, 매분 실행되는 monitor.sh가 이미 있으므로 자체 구현이 효율적이라 판단.

---

## 항목 3 — 보안/운영 원칙

### 3-1. SSH 포트 변경 + Root 차단이 왜 보안에 효과적인가 (위협 모델 관점)

**위협 모델**

- **자동화된 무차별 스캔**: 인터넷 봇넷은 22번 포트만 노린 ID/암호 사전 공격을 **시간당 수천 회** 시도한다.
- **자격 증명 유출 시 즉시 root 권한 탈취**: `root@host` 접속이 허용되면 단 한 번의 root 비밀번호 유출로 시스템 전체가 장악됨.

**대응 메커니즘**

| 조치 | 효과 |
|---|---|
| 포트 22 → 20022 | 자동 스캔 트래픽의 99% 회피 (대부분 봇이 22번만 노림). 로그 노이즈 격감 → 진짜 공격 탐지 쉬워짐 |
| `PermitRootLogin no` | 공격자가 root 계정 비밀번호를 얻어도 SSH로는 직접 들어올 수 없음[^8]. **반드시 일반 사용자 → `sudo`** 2단계 강제 |
| 결과 | "**누가**(일반 계정) 무엇을 했는지 `/var/log/auth.log`에 남음" → 사후 추적·차단 근거 확보 |

**한계도 짚어두기**: 포트 변경은 **security through obscurity** (감추기에 의한 보안)에 가까워 표적 공격엔 효과 제한적. 그래서 `fail2ban`, `key-only auth` 같은 추가 layer가 일반적이며, 이 미션에선 비밀번호 인증을 굳이 끊지 않았으나 다음 단계 보완 사항이다.

### 3-2. api_keys · 로그를 agent-core로 제한한 이유 — 최소 권한 원칙

**최소 권한 원칙(Principle of Least Privilege, PoLP)**: 어떤 주체에게도 **그 작업을 수행하는 데 꼭 필요한 최소한의 권한**만 부여한다[^9].

**그룹 분리 매트릭스**

| 자원 | 누가 접근? | 어떤 그룹으로 제한? |
|---|---|---|
| 업로드 파일 (`upload_files/`) | admin/dev/test 모두 협업 | **agent-common** (넓은 그룹) |
| API 키 (`api_keys/`) | 운영(admin) + 개발(dev)만, test는 X | **agent-core** (좁은 그룹) |
| 운영 로그 (`/var/log/agent-app/`) | 모니터링·분석 권한자만 | **agent-core** |

**효과**

- `agent-test` 계정은 agent-common에는 있지만 **agent-core엔 없으므로** API 키와 로그를 OS 차원에서 **자동 차단**된다.
- 코드 안에 별도 권한 체크를 안 넣어도 OS 권한 시스템이 강제. **방어 계층(defense in depth)** 확보.
- 키 유출·로그 변조 사고가 나도 **영향 범위(blast radius)**가 좁은 그룹으로 한정됨.

### 3-3. "경고는 출력하되 종료하지 않는 항목"을 분리한 운영상의 이유

monitor.sh에서 **종료(`exit 1`)**와 **경고(`WARNING`만 출력)**를 다음 기준으로 분리:

| 분류 | 항목 | 동작 |
|---|---|---|
| **즉시 종료 (exit 1)** | 앱 프로세스 미존재, 15034 포트 LISTEN 아님 | 서비스 자체가 죽었거나 네트워크로 접근 불가 → **서비스 중단 상태** |
| **경고만 출력 (계속 진행)** | 방화벽 비활성, CPU/MEM/DISK 임계치 초과 | 서비스는 살아있고, 즉시 조치는 필요하지만 **모니터링 자체는 계속 돼야 함** |

**운영상의 이유**

1. **모니터링의 연속성**: 임계치 초과 시 스크립트가 죽으면 그 순간의 자원 사용량 기록이 `monitor.log`에 안 남는다. **장애를 분석할 단서가 사라짐**.
2. **장애 정의의 차이**: "프로세스/포트 없음"은 **서비스 다운**이고, "CPU 90% 초과"는 **성능 경고**이다. 둘을 같은 무게로 다루면 알람이 과민해진다.
3. **알람 시스템 연동**: cron 종료 코드(`exit 1`)는 외부 알림 시스템과 연동되기 쉬움. 정말 중요한 것(서비스 다운)만 `exit 1`을 내야 알람이 신뢰성을 가짐.
4. **사후 분석 자료 누적**: 경고도 `monitor.log`에 기록되므로 시계열로 보면 "CPU가 며칠째 임계 근처"라는 추세를 발견 가능.

### 3-4. `>` 와 `>>` 차이, 로그 누적에 `>>`가 필요한 이유

| 연산자 | 동작 | 결과 |
|---|---|---|
| `>` | 표준출력을 파일에 **덮어쓰기** | 파일이 있으면 비우고 처음부터 다시 씀. 이전 내용 손실.[^10] |
| `>>` | 표준출력을 파일에 **이어 쓰기 (append)** | 파일이 있으면 끝에 추가. 이전 내용 보존. |

**예시**

```bash
echo "first"  > out.log    # out.log = "first"
echo "second" > out.log    # out.log = "second"  ← first가 사라짐
echo "third" >> out.log    # out.log = "second\nthird"  ← 추가됨
```

**로그 누적에 `>>`가 필요한 이유**

- 로그는 **시계열 누적 기록**이다. 매분 cron이 monitor.sh를 호출하는데, 만약 `>`를 쓰면 매분 이전 분의 기록이 사라져 **마지막 1줄만 남는다**.
- monitor.sh §5의 `echo "${LOG_LINE}" >> "${LOG_FILE}"`이 그래서 반드시 `>>`여야 함.
- 분석 도구(`report.sh`, `wc -l`, `tail`, `awk`)도 모두 "여러 줄"을 전제로 동작.
- 마찬가지로 crontab에 적은 `>> /home/agent-admin/monitor.cron.log 2>&1`도 cron 표준출력·에러를 매분 누적하기 위함.

---

## 항목 4 — 응용/확장

### 4-1. 모니터링 대상이 Nginx로 바뀐다면 monitor.sh의 핵심 변경 포인트

#### 들어가기 전에 — Nginx 가 뭔가

> **Nginx** ("엔진엑스") 는 세상에서 가장 많이 쓰이는 **웹 서버** 중 하나다.
> 누군가 브라우저에서 `https://example.com` 을 열면, 그 요청을 받아 HTML/이미지를 돌려보내거나, 뒤단의 다른 앱(파이썬·자바 등) 으로 요청을 넘겨주는 역할.

우리 미션의 `agent-app` 과 비교해 보면 — **둘 다 "포트에서 듣고 있는 서버 프로그램"** 이라는 점은 같다. 그래서 monitor.sh 의 **큰 구조(Health → Resource → Threshold → Log → Rotate)** 는 그대로 재사용할 수 있고, **세부 항목만** 갈아 끼우면 된다. 어떤 부분이 어떻게 바뀌는지 하나씩 보자.

#### 한눈에 보는 비교표

| 영역 | 현재 (agent-app) | Nginx 로 바꾸면 |
|------|------------------|----------------|
| 프로세스 이름 | `agent-app` | `nginx` (보통 master + worker 여러 개) |
| 듣는 포트 | 15034 (단일 TCP) | 80 (HTTP) + 443 (HTTPS) 두 개 |
| 동작 확인 방법 | "포트가 열려 있나" | "실제로 HTTP 응답을 주나"(200 OK) |
| 로그 파일 | `monitor.log` (우리가 직접 누적) | `access.log` + `error.log` (Nginx 가 자동으로 씀) |
| 추가 지표 | CPU/MEM/DISK | 위 + 요청 수, 5xx 비율, 워커 개수 |
| 관리 방법 | 직접 `nohup ./agent-app &` | `systemctl start/stop/reload nginx` |

이제 각 항목을 풀어 설명한다.

---

#### (1) 프로세스 식별 — 같은 `pgrep` 이지만 한 단계 더

**현재 monitor.sh 에서**:

```bash
APP_PID="$(pgrep -x "agent-app" | head -n1)"
```

`pgrep -x agent-app` 은 이름이 정확히 `agent-app` 인 프로세스의 PID 를 찾는다. agent-app 은 우리가 직접 띄운 단일 프로세스라 단순.

**Nginx 로 바꾸면 두 가지 옵션**:

```bash
# 옵션 A — 동일한 pgrep 패턴
NGINX_PID="$(pgrep -x nginx | head -n1)"

# 옵션 B — systemd 서비스 상태 직접 질의 (Nginx 는 보통 systemd 서비스로 동작)
if systemctl is-active --quiet nginx; then
    echo "Nginx is running"
fi
```

**왜 옵션 B 를 권장하나** — Nginx 는 보통 `master process` 와 여러 `worker process` 로 구성된다[^11].

```
$ pgrep -a nginx
1234 nginx: master process /usr/sbin/nginx
1235 nginx: worker process
1236 nginx: worker process
1237 nginx: worker process
1238 nginx: worker process
```

`pgrep -x nginx` 은 이 5개를 모두 잡아서 첫 PID 를 반환하는데, 어떤 게 잡힐지 매번 달라질 수 있다. 반면 `systemctl is-active` 는 **"서비스가 살아 있는가"** 라는 본질적 질문에 답하므로 더 안정적.

→ Nginx 처럼 **systemd 가 관리하는 서비스**라면 `systemctl is-active` 가 더 정확.

---

#### (2) 포트 — 단일에서 복수로

**현재**: 15034 한 개만 확인.

```bash
sudo ss -tlnH | awk '$4 ~ /:15034$/ {f=1} END{exit !f}'
```

**Nginx 로 바꾸면**: 일반적으로 **HTTP 80 + HTTPS 443** 두 개를 동시에 듣는다. 둘 다 LISTEN 인지 확인해야 한다.

```bash
PORTS_OK=0
for port in 80 443; do
    if sudo ss -tlnH | awk -v p=":$port$" '$4 ~ p {f=1} END{exit !f}'; then
        echo "Port $port LISTEN [OK]"
    else
        echo "Port $port not LISTEN [FAIL]"
        PORTS_OK=1
    fi
done
[[ "$PORTS_OK" != "0" ]] && exit 1
```

**왜 둘 다 봐야 하나** — HTTP 만 살아있고 HTTPS 가 죽으면 보안 접속을 못 하는 상태다. 사용자 입장에선 "사이트가 안 열린다" 라는 같은 증상이지만, 원인 진단을 빨리 하려면 어느 쪽이 죽었는지 분리해서 봐야 한다.

---

#### (3) 헬스체크 — "포트만 열려 있나" → "진짜 응답하나"

이게 가장 큰 차이점이다. 한번 짚고 가자.

**포트가 열려 있다 ≠ 정상 동작 중이다**

다음 같은 상황을 생각해 보자:

```
포트 80 은 열려 있다 ✓
그런데 Nginx 가 모든 요청에 500 Internal Server Error 만 돌려준다 ✗
→ ss 로는 "LISTEN [OK]" 로 보이지만, 사용자는 사이트 못 씀.
```

이런 미묘한 고장(전문용어로 "**partial failure**") 을 잡으려면 **실제 요청을 보내** 응답을 봐야 한다.

```bash
# curl 로 실제 HTTP 응답 코드 받아오기
HTTP_CODE="$(curl -fsS -o /dev/null -w '%{http_code}' \
              --connect-timeout 3 --max-time 5 \
              http://127.0.0.1/ || echo 000)"

if [[ "$HTTP_CODE" == "200" ]]; then
    echo "HTTP health [OK]"
elif [[ "$HTTP_CODE" == "000" ]]; then
    echo "HTTP health [FAIL] — 연결 자체 실패 (Nginx 죽음 / 방화벽)"
    exit 1
else
    echo "HTTP health [WARN] — 응답 코드 $HTTP_CODE"
fi
```

**`curl` 옵션 풀이** (자주 헷갈리는 부분):

| 옵션 | 의미 |
| ---- | ---- |
| `-f` | HTTP 4xx/5xx 면 `curl` 자체가 실패로 끝나 (`exit 22`)[^12] — 종료코드로 분기 가능 |
| `-s` | 진행률 표시 같은 "잡음" 출력 안 함 |
| `-S` | 에러 메시지는 그래도 출력 (silent 모드에서도) |
| `-o /dev/null` | 응답 본문은 화면이 아닌 휴지통으로 — HTML 이 콘솔로 쏟아지지 않음 |
| `-w '%{http_code}'` | "응답 헤더에서 HTTP 상태 코드만 추출해 출력해 줘" |
| `--connect-timeout 3` | 3초 안에 TCP 연결 안 되면 포기 (네트워크 멈춤 방지)[^13] |
| `--max-time 5` | 전체 요청이 5초 넘으면 포기 |
| `\|\| echo 000` | curl 자체가 실패하면 `000` 을 출력 → 분기 처리용 placeholder |

→ 이렇게 하면 "포트는 열렸지만 실제론 망가진" 상태까지 잡을 수 있다.

---

#### (4) 로그 — "직접 만든 1개" → "Nginx 가 만드는 2개"

**현재**: monitor.sh 가 직접 `/var/log/agent-app/monitor.log` 에 한 줄씩 누적한다 (스크립트가 만들어서 쓰는 로그).

**Nginx 로 바꾸면**: Nginx 가 자체적으로 두 종류의 로그를 자동 생성한다.

| 파일 | 용도 | 예시 한 줄 |
|------|------|----------|
| `/var/log/nginx/access.log` | 접속 요청 1건당 1줄 — "누가 언제 어디로 요청했고 응답은 뭐였나" | `192.168.1.5 - - [17/May/2026:18:23:11 +0900] "GET / HTTP/1.1" 200 1234 "-" "Mozilla/5.0"` |
| `/var/log/nginx/error.log` | 에러·경고 — 설정 문제, upstream 죽음 등 | `2026/05/17 18:23:42 [error] 1235#0: *17 connect() failed (111: Connection refused)` |

monitor.sh 입장에선 **이 두 파일을 "읽어서 분석"** 하는 쪽으로 역할이 바뀐다.

```bash
# 최근 1000건 중 5xx 에러 응답 수 세기
RECENT_5XX=$(sudo tail -n 1000 /var/log/nginx/access.log \
             | awk '$9 ~ /^5/ {c++} END {print c+0}')

if [[ "$RECENT_5XX" -gt 10 ]]; then
    echo "[WARNING] 최근 1000건 중 5xx 응답이 $RECENT_5XX 건"
fi
```

**한 줄씩 설명**:

- `tail -n 1000 access.log` : 최근 1000줄만 보기 (전체 파일은 너무 크니까)
- `awk '$9 ~ /^5/ {c++}'` : 9번째 컬럼(HTTP 상태 코드)이 `5` 로 시작하면 카운터 증가[^14]
- `END {print c+0}` : 마지막에 카운트 출력. `c+0` 은 "비어있으면 0으로" 처리.
- `[[ "$RECENT_5XX" -gt 10 ]]` : 10건 초과면 경고

5xx 비율이 갑자기 튀면 → 백엔드 죽음 / 디스크 가득 / DB 다운 등 신호. 우리 임계값 경고 패턴(CPU>20%) 과 같은 흐름.

---

#### (5) 임계값 — Nginx 만의 새 지표

**현재 monitor.sh 의 임계값**:

```
CPU > 20%   → WARNING
MEM > 10%   → WARNING
DISK > 80%  → WARNING
```

CPU/MEM/DISK 는 운영체제 차원의 보편적 지표라 **Nginx 로 바꿔도 그대로** 가져간다. **추가로 챙길 Nginx 고유 지표**가 있다.

| 새 지표 | 측정 방법 | 임계값 예 |
|--------|----------|---------|
| **5xx 응답 비율** | 위에서 본 `awk '$9 ~ /^5/'` | 1% 초과 시 WARNING |
| **분당 요청 수 (RPS)** | `awk '$4 ~ /현재시각/'` | 평소의 10배 초과 시 (DDoS 의심) |
| **워커 프로세스 수** | `pgrep -c nginx` (master + worker 총 개수) | 설정값과 불일치 시 |
| **upstream 타임아웃** | `grep "upstream timed out" error.log` | 분당 5건 초과 |

```bash
# 워커 수 점검 예시
EXPECTED_WORKERS=4
ACTUAL=$(pgrep -c nginx)   # master 1개 포함
if [[ "$ACTUAL" -ne $((EXPECTED_WORKERS + 1)) ]]; then
    echo "[WARNING] worker 개수 이상 (예상 $((EXPECTED_WORKERS+1)) / 실제 $ACTUAL)"
fi
```

---

#### 종합 — 어떤 부분이 안 바뀌나

핵심은 "**구조는 그대로, 세부만 교체**" 다. monitor.sh 전체를 새로 쓸 필요가 없다.

```
┌─────────────────────────────────────────────────────────┐
│  [구조]  Health Check → Resource → Threshold            │
│             → Log Append → Rotate                       │
│                                                          │
│  [재사용 가능 — 그대로]                                  │
│    • set -u, set -o pipefail                            │
│    • CPU/MEM/DISK 추출 awk 한 줄                        │
│    • [WARNING] 출력 패턴                                │
│    • 로그 로테이션 로직 (10MB × 10개)                   │
│    • exit 1 vs WARNING 분기 정책                        │
│                                                          │
│  [갈아 끼우는 곳 — 변수·명령 4~5 군데]                  │
│    • APP_NAME=agent-app    → APP_NAME=nginx             │
│    • APP_PORT=15034         → APP_PORTS=(80 443)        │
│    • 단순 LISTEN 체크       → curl HTTP 응답 체크 추가  │
│    • monitor.log 쓰기       → access/error.log 읽기 추가│
│    • (선택) 워커 수 점검 추가                            │
└─────────────────────────────────────────────────────────┘
```

이게 monitor.sh 같은 **잘 구조화된 자동화 스크립트의 본질적 가치** 다 — 한 번 잘 짜두면 모니터링 대상이 바뀌어도 80% 는 재사용. **"무엇" 만 바꾸고 "어떻게" 는 안 바꿔도 된다.**

---

#### 한 줄 요약

> **프로세스명·포트·헬스체크 방식·로그 위치·임계값 정의** 다섯 가지가 핵심 교체 포인트.
> 구조(Health → Resource → Threshold → Log → Rotate) 는 그대로.
> Nginx 같은 systemd 서비스라면 `systemctl is-active` + `curl` 응답 검증을 함께 쓰는 게 정석.

### 4-2. 프로세스는 살아있는데 포트가 안 열리는 상황 — 원인 후보 + 확인 순서

이건 흔한 trouble case이고 진단 순서가 중요하다.

**원인 후보 (가능성 순)**

1. **포트 바인딩 실패** — 다른 프로세스가 같은 포트를 선점.
2. **부팅 단계 지연** — 프로세스는 떴지만 LISTEN 준비 전 (DB 연결 등 대기 중).
3. **잘못된 인터페이스 바인딩** — `127.0.0.1:15034`만 듣는데 외부에서 접근.
4. **방화벽 차단** — 프로세스는 LISTEN 중이지만 UFW/iptables가 차단.
5. **권한 문제** — 1024 미만 포트를 비-root가 binding 시도 → 권한 거부.
6. **앱 내부 에러** — 시작했다가 listen 직전 crash, supervisor가 재시작 중인 상태.

**확인 순서**

```bash
# 1. 프로세스 진짜 살아있는지 (좀비/멈춤 아닌지)
ps -eo pid,stat,cmd | grep agent-app
# STAT 컬럼: R(running) / S(sleeping)은 정상, Z(zombie) / D(uninterruptible)면 문제

# 2. 포트가 실제로 어떤 프로세스/인터페이스에 묶였는지
sudo ss -tlnp | grep 15034
# 결과 없음 → 아무도 LISTEN 안 함
# 127.0.0.1:15034 → loopback만 → 외부 접근 불가

# 3. 같은 포트를 다른 프로세스가 잡고 있는지
sudo lsof -i :15034
# 또는 sudo fuser -n tcp 15034

# 4. 앱 로그에서 listen 실패 메시지 확인
journalctl -u agent-app -n 50          # systemd 서비스인 경우
# 또는 직접 로그 파일
tail -n 100 /var/log/agent-app/app.log

# 5. 방화벽 차단 여부
sudo ufw status numbered
sudo iptables -L -n | grep 15034

# 6. SELinux/AppArmor 같은 강제 접근제어 (있다면)
sudo dmesg | tail -50
```

진단 결과에 따라:
- "LISTEN 자체가 없음" → 앱 로그 → 코드 결함 또는 의존성 문제
- "127.0.0.1로만 LISTEN" → 앱 설정 (`bind = '0.0.0.0'` 등) 변경
- "다른 PID가 점유" → 그 프로세스 종료 또는 포트 변경
- "LISTEN OK인데 외부 안 됨" → 방화벽 / 네트워크 ACL

### 4-3. 로그가 급증해 디스크가 가득 찰 위험 — 단기 / 중기 대응

**단기 (5분 내 조치)**

```bash
# 1. 디스크 사용량 즉시 파악
df -h /var
sudo du -sh /var/log/* | sort -h | tail -10

# 2. 가장 큰 로그 파일 비우기 (truncate — 프로세스 핸들 유지)
sudo truncate -s 0 /var/log/agent-app/monitor.log
# rm 으로 지우면 프로세스가 여전히 file descriptor를 잡고 있어 디스크가 안 비워짐.
# truncate -s 0 는 안전하게 0 바이트로 만든다.

# 3. 보관 가능한 로그는 즉시 압축
sudo gzip /var/log/agent-app/monitor.log.{1..9} 2>/dev/null
```

**중기 (당일~주간 조치)**

1. **로그 로테이션 정책 강화**
   - 현재 10MB × 10개 = 100MB 한계 → 더 짧은 주기로 (예: 5MB × 5개)
   - 또는 [archive_logs.sh](../../src/archive_logs.sh)로 **7일 이후 gzip 압축**, 30일 이후 삭제 정책 강제

2. **로그 레벨 검토**
   - DEBUG/INFO 폭주가 원인이라면 운영에선 WARN 이상만 기록하도록 앱 설정 변경
   - monitor.sh는 한 줄/분이라 자체 폭주 가능성 낮지만, agent-app의 로그가 원인일 수 있음

3. **별도 파티션 분리**
   - `/var/log`를 별도 LVM/마운트로 분리 → 다른 시스템 영역에 영향 없게

4. **모니터링 알람 추가**
   - monitor.sh의 DISK 임계값을 80% → 70%로 낮추고, 90% 이상 시 cron이 자동으로 archive_logs.sh를 호출하도록 chain
   - 외부 알림 (메일, 슬랙) 연계해 디스크 75% 시점에 운영자 알림

5. **로그 외부 전송**
   - 장기 보존이 필요하면 rsyslog/logstash로 원격 수집 서버에 전송 → 로컬에는 단기만 보관

---

## 항목 5 — 보너스 (크레딧 100)

### 보너스 1 — report.sh: 통계 리포트

[report.sh](../../src/report.sh)는 monitor.log를 후처리해 평균/최대/최소를 출력한다.

- 인자 없이 실행: 전체 로그 통계
- 두 시각 인자: 그 구간만 통계
  ```bash
  ./report.sh "2026-05-11 12:00:00" "2026-05-11 13:00:00"
  ```
- 핵심 도구: `awk`로 한 번에 합/개수/최대/최소 산출. 외부 의존성 0.

### 보너스 2 — archive_logs.sh: 시간 기반 보존 정책

[archive_logs.sh](../../src/archive_logs.sh)는 cron으로 매일 03:10에 실행되며:

- 7일 경과 `*.log` → `gzip` 후 `/var/log/monitor/agent-app/archive/`로 이동
- 30일 경과 `*.gz` 삭제
- 디렉토리 미존재/권한 부족/대상 0개 등 예외 상황에서 안전 종료(WARNING 출력 후 exit 0)

> **운영상의 의미**: monitor.sh의 size-based rotation(즉시성)과 archive_logs.sh의 time-based archive(보존)가 **2단 방어**를 이룬다. 폭주는 monitor.sh가 막고, 일상 누적은 archive_logs.sh가 정리.

---

## 참고 — 핵심 파일 위치

| 파일 | 역할 | 위치 |
|---|---|---|
| `monitor.sh` | 분당 모니터링 + 로그 + 로테이션 | `$AGENT_HOME/bin/monitor.sh` |
| `agent-app` | 데모 애플리케이션 (Boot Sequence + LISTEN) | `$AGENT_HOME/agent-app` |
| `report.sh` | 로그 통계 리포트 | `$AGENT_HOME/bin/report.sh` |
| `archive_logs.sh` | 시간 기반 압축·삭제 | `$AGENT_HOME/bin/archive_logs.sh` |
| `01_*.sh ~ 07_*.sh` | 환경 셋업 7 단계 (SSH·UFW·계정·ACL·env·deploy·cron) | `src/` |
| `00_run_all.sh` | 01~07 일괄 실행 wrapper | `src/` |
| `요구사항_수행_내역서.md` | 단계별 셋업·검증 가이드 | (작업 디렉토리 루트) |

---

## 출처 & 참고 문헌 (Sources & References)

> 본문 위첨자 각주 번호를 누르면 아래 출처로, ↩ 로 본문 위치로 돌아온다. `⚠` 는 검증 중 발견한 보충/정정. 출처는 1차/표준 자료 우선(man7.org · GNU · PyInstaller · docs.python.org · Wikipedia 등).

[^1]: [crontab(5) — man7.org](https://man7.org/linux/man-pages/man5/crontab.5.html)
[^2]: [pgrep(1) — man7.org](https://man7.org/linux/man-pages/man1/pgrep.1.html)
[^3]: [ss(8) — man7.org](https://man7.org/linux/man-pages/man8/ss.8.html)
[^4]: [ss(8) — man7.org](https://man7.org/linux/man-pages/man8/ss.8.html)
[^5]: [top(1) — man7.org](https://man7.org/linux/man-pages/man1/top.1.html)
[^6]: [bash(1) — CONDITIONAL EXPRESSIONS](https://man7.org/linux/man-pages/man1/bash.1.html)
[^7]: [setfacl(1) — man7.org](https://man7.org/linux/man-pages/man1/setfacl.1.html) · [acl(5) — man7.org](https://man7.org/linux/man-pages/man5/acl.5.html)
[^8]: [sshd_config(5) — PermitRootLogin](https://man.openbsd.org/sshd_config)
[^9]: [Principle of least privilege — Wikipedia](https://en.wikipedia.org/wiki/Principle_of_least_privilege)
[^10]: [bash(1) — REDIRECTION (>, >>)](https://man7.org/linux/man-pages/man1/bash.1.html)
[^11]: [Inside NGINX (NGINX Blog)](https://blog.nginx.org/blog/inside-nginx-how-we-designed-for-performance-scale)
[^12]: [curl man page (--fail)](https://curl.se/docs/manpage.html) · [everything curl — exit code](https://everything.curl.dev/cmdline/exitcode.html)
[^13]: [curl man page (--connect-timeout)](https://curl.se/docs/manpage.html)
[^14]: [GNU Awk — Field references](https://www.gnu.org/software/gawk/manual/html_node/Fields.html)
